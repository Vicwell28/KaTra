"use strict";

/**
 * Convierte un número en su forma expandida como una cadena de manera concisa.
 *
 * @param {number} num - El número que se convertirá en forma expandida.
 * @returns {string} - El número en forma expandida.
 *
 * @example
 * // returns "70000 + 300 + 4"
 * expandedForm(70304);
 *
 * @example
 * // returns "10 + 2"
 * expandedForm(12);
 *
 * @example
 * // returns "40 + 5"
 * expandedForm(45);
 */
w
// Ejemplos de uso
console.log(expandedForm(70304)); // "70000 + 300 + 4"
console.log(expandedForm(12));    // "10 + 2"
console.log(expandedForm(45));    // "40 + 5"